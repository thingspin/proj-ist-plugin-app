export const STEPS = {
    project: 'project',
    platform: 'platform',
    model: 'model',
    algorithm: 'algorithm',
    result: 'result'
};

export interface Step {
    step: any;
    valid: Boolean;
}
