export interface Step1Struct { // Mqtt sub(ip, port topic)
    ip: String;
    port: Number;
    topic: String;
}

export interface Step2Struct {
    framework: String;
}

export interface Step3Struct { // Unknown
}

export interface Step4Struct { // Unknown
}

export interface Step5Struct { // Mqtt sub(ip, port topic)
    ip: String;
    port: Number;
    topic: String;
}
